# Setup Guide for jubilujah.com

## Prerequisites
- Node.js 20+
- npm 10+

## Development
1. Install dependencies: `cd web && npm install && cd ../api && npm install`
2. Start servers: `npm run dev` in each directory
